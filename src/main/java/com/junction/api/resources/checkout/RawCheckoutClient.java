/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.checkout;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.MediaTypes;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.RequestOptions;
import com.junction.api.core.RetryInterceptor;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.checkout.requests.ConfirmCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.CreateCheckoutQuoteBody;
import com.junction.api.resources.checkout.requests.GetCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.GetQuoteCheckoutRequest;
import com.junction.api.resources.checkout.requests.RefineCheckoutQuoteBody;
import com.junction.api.types.CheckoutQuote;
import com.junction.api.types.CheckoutSession;
import com.junction.api.types.HttpValidationError;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCheckoutClient {
    protected final ClientOptions clientOptions;

    public RawCheckoutClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public JunctionHttpResponse<CheckoutSession> getCheckoutSession(String checkoutSessionId) {
        return getCheckoutSession(
                checkoutSessionId, GetCheckoutSessionCheckoutRequest.builder().build());
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public JunctionHttpResponse<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, RequestOptions requestOptions) {
        return getCheckoutSession(
                checkoutSessionId, GetCheckoutSessionCheckoutRequest.builder().build(), requestOptions);
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public JunctionHttpResponse<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, GetCheckoutSessionCheckoutRequest request) {
        return getCheckoutSession(checkoutSessionId, request, null);
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public JunctionHttpResponse<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, GetCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout/v1/session")
                .addPathSegment(checkoutSessionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutSession.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public JunctionHttpResponse<CheckoutSession> confirmCheckoutSession(String checkoutSessionId) {
        return confirmCheckoutSession(
                checkoutSessionId,
                ConfirmCheckoutSessionCheckoutRequest.builder().build());
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public JunctionHttpResponse<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, RequestOptions requestOptions) {
        return confirmCheckoutSession(
                checkoutSessionId,
                ConfirmCheckoutSessionCheckoutRequest.builder().build(),
                requestOptions);
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public JunctionHttpResponse<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request) {
        return confirmCheckoutSession(checkoutSessionId, request, null);
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public JunctionHttpResponse<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout/v1/session")
                .addPathSegment(checkoutSessionId)
                .addPathSegments("confirm");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutSession.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<CheckoutQuote> createQuote(CreateCheckoutQuoteBody request) {
        return createQuote(request, null);
    }

    public JunctionHttpResponse<CheckoutQuote> createQuote(
            CreateCheckoutQuoteBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout/v1/quote");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutQuote.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<CheckoutQuote> refineQuote(String quoteId) {
        return refineQuote(quoteId, RefineCheckoutQuoteBody.builder().build());
    }

    public JunctionHttpResponse<CheckoutQuote> refineQuote(String quoteId, RequestOptions requestOptions) {
        return refineQuote(quoteId, RefineCheckoutQuoteBody.builder().build(), requestOptions);
    }

    public JunctionHttpResponse<CheckoutQuote> refineQuote(String quoteId, RefineCheckoutQuoteBody request) {
        return refineQuote(quoteId, request, null);
    }

    public JunctionHttpResponse<CheckoutQuote> refineQuote(
            String quoteId, RefineCheckoutQuoteBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout/v1/quote")
                .addPathSegment(quoteId)
                .addPathSegments("refine");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutQuote.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<CheckoutQuote> getQuote(String quoteId) {
        return getQuote(quoteId, GetQuoteCheckoutRequest.builder().build());
    }

    public JunctionHttpResponse<CheckoutQuote> getQuote(String quoteId, RequestOptions requestOptions) {
        return getQuote(quoteId, GetQuoteCheckoutRequest.builder().build(), requestOptions);
    }

    public JunctionHttpResponse<CheckoutQuote> getQuote(String quoteId, GetQuoteCheckoutRequest request) {
        return getQuote(quoteId, request, null);
    }

    public JunctionHttpResponse<CheckoutQuote> getQuote(
            String quoteId, GetQuoteCheckoutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("checkout/v1/quote")
                .addPathSegment(quoteId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutQuote.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }
}
