/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.checkout;

import com.junction.api.core.ClientOptions;
import com.junction.api.core.RequestOptions;
import com.junction.api.resources.checkout.requests.ConfirmCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.CreateCheckoutQuoteBody;
import com.junction.api.resources.checkout.requests.GetCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.GetQuoteCheckoutRequest;
import com.junction.api.resources.checkout.requests.RefineCheckoutQuoteBody;
import com.junction.api.types.CheckoutQuote;
import com.junction.api.types.CheckoutSession;
import java.util.concurrent.CompletableFuture;

public class AsyncCheckoutClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawCheckoutClient rawClient;

    public AsyncCheckoutClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawCheckoutClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawCheckoutClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CompletableFuture<CheckoutSession> getCheckoutSession(String checkoutSessionId) {
        return this.rawClient.getCheckoutSession(checkoutSessionId).thenApply(response -> response.body());
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CompletableFuture<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, RequestOptions requestOptions) {
        return this.rawClient
                .getCheckoutSession(checkoutSessionId, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CompletableFuture<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, GetCheckoutSessionCheckoutRequest request) {
        return this.rawClient.getCheckoutSession(checkoutSessionId, request).thenApply(response -> response.body());
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CompletableFuture<CheckoutSession> getCheckoutSession(
            String checkoutSessionId, GetCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getCheckoutSession(checkoutSessionId, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CompletableFuture<CheckoutSession> confirmCheckoutSession(String checkoutSessionId) {
        return this.rawClient.confirmCheckoutSession(checkoutSessionId).thenApply(response -> response.body());
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CompletableFuture<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, RequestOptions requestOptions) {
        return this.rawClient
                .confirmCheckoutSession(checkoutSessionId, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CompletableFuture<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request) {
        return this.rawClient.confirmCheckoutSession(checkoutSessionId, request).thenApply(response -> response.body());
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CompletableFuture<CheckoutSession> confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .confirmCheckoutSession(checkoutSessionId, request, requestOptions)
                .thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> createQuote(CreateCheckoutQuoteBody request) {
        return this.rawClient.createQuote(request).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> createQuote(
            CreateCheckoutQuoteBody request, RequestOptions requestOptions) {
        return this.rawClient.createQuote(request, requestOptions).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> refineQuote(String quoteId) {
        return this.rawClient.refineQuote(quoteId).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> refineQuote(String quoteId, RequestOptions requestOptions) {
        return this.rawClient.refineQuote(quoteId, requestOptions).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> refineQuote(String quoteId, RefineCheckoutQuoteBody request) {
        return this.rawClient.refineQuote(quoteId, request).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> refineQuote(
            String quoteId, RefineCheckoutQuoteBody request, RequestOptions requestOptions) {
        return this.rawClient.refineQuote(quoteId, request, requestOptions).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> getQuote(String quoteId) {
        return this.rawClient.getQuote(quoteId).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> getQuote(String quoteId, RequestOptions requestOptions) {
        return this.rawClient.getQuote(quoteId, requestOptions).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> getQuote(String quoteId, GetQuoteCheckoutRequest request) {
        return this.rawClient.getQuote(quoteId, request).thenApply(response -> response.body());
    }

    public CompletableFuture<CheckoutQuote> getQuote(
            String quoteId, GetQuoteCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient.getQuote(quoteId, request, requestOptions).thenApply(response -> response.body());
    }
}
