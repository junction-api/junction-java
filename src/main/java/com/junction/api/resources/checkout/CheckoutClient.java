/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.checkout;

import com.junction.api.core.ClientOptions;
import com.junction.api.core.RequestOptions;
import com.junction.api.resources.checkout.requests.ConfirmCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.CreateCheckoutQuoteBody;
import com.junction.api.resources.checkout.requests.GetCheckoutSessionCheckoutRequest;
import com.junction.api.resources.checkout.requests.GetQuoteCheckoutRequest;
import com.junction.api.resources.checkout.requests.RefineCheckoutQuoteBody;
import com.junction.api.types.CheckoutQuote;
import com.junction.api.types.CheckoutSession;

public class CheckoutClient {
    protected final ClientOptions clientOptions;

    private final RawCheckoutClient rawClient;

    public CheckoutClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawCheckoutClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawCheckoutClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CheckoutSession getCheckoutSession(String checkoutSessionId) {
        return this.rawClient.getCheckoutSession(checkoutSessionId).body();
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CheckoutSession getCheckoutSession(String checkoutSessionId, RequestOptions requestOptions) {
        return this.rawClient
                .getCheckoutSession(checkoutSessionId, requestOptions)
                .body();
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CheckoutSession getCheckoutSession(String checkoutSessionId, GetCheckoutSessionCheckoutRequest request) {
        return this.rawClient.getCheckoutSession(checkoutSessionId, request).body();
    }

    /**
     * Get the checkout session snapshot.
     * <p>In-flight sessions reflect live workflow state; terminal sessions are
     * served from the persisted snapshot. A read arriving moments after session
     * creation can 404 until the workflow's first persistence write commits.</p>
     */
    public CheckoutSession getCheckoutSession(
            String checkoutSessionId, GetCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getCheckoutSession(checkoutSessionId, request, requestOptions)
                .body();
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CheckoutSession confirmCheckoutSession(String checkoutSessionId) {
        return this.rawClient.confirmCheckoutSession(checkoutSessionId).body();
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CheckoutSession confirmCheckoutSession(String checkoutSessionId, RequestOptions requestOptions) {
        return this.rawClient
                .confirmCheckoutSession(checkoutSessionId, requestOptions)
                .body();
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CheckoutSession confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request) {
        return this.rawClient.confirmCheckoutSession(checkoutSessionId, request).body();
    }

    /**
     * Confirm the checkout session after completing payment.
     * <p>In-flight sessions verify payment against the channel (Stripe) and report
     * order progress; terminal sessions return the persisted snapshot untouched.</p>
     */
    public CheckoutSession confirmCheckoutSession(
            String checkoutSessionId, ConfirmCheckoutSessionCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .confirmCheckoutSession(checkoutSessionId, request, requestOptions)
                .body();
    }

    public CheckoutQuote createQuote(CreateCheckoutQuoteBody request) {
        return this.rawClient.createQuote(request).body();
    }

    public CheckoutQuote createQuote(CreateCheckoutQuoteBody request, RequestOptions requestOptions) {
        return this.rawClient.createQuote(request, requestOptions).body();
    }

    public CheckoutQuote refineQuote(String quoteId) {
        return this.rawClient.refineQuote(quoteId).body();
    }

    public CheckoutQuote refineQuote(String quoteId, RequestOptions requestOptions) {
        return this.rawClient.refineQuote(quoteId, requestOptions).body();
    }

    public CheckoutQuote refineQuote(String quoteId, RefineCheckoutQuoteBody request) {
        return this.rawClient.refineQuote(quoteId, request).body();
    }

    public CheckoutQuote refineQuote(String quoteId, RefineCheckoutQuoteBody request, RequestOptions requestOptions) {
        return this.rawClient.refineQuote(quoteId, request, requestOptions).body();
    }

    public CheckoutQuote getQuote(String quoteId) {
        return this.rawClient.getQuote(quoteId).body();
    }

    public CheckoutQuote getQuote(String quoteId, RequestOptions requestOptions) {
        return this.rawClient.getQuote(quoteId, requestOptions).body();
    }

    public CheckoutQuote getQuote(String quoteId, GetQuoteCheckoutRequest request) {
        return this.rawClient.getQuote(quoteId, request).body();
    }

    public CheckoutQuote getQuote(String quoteId, GetQuoteCheckoutRequest request, RequestOptions requestOptions) {
        return this.rawClient.getQuote(quoteId, request, requestOptions).body();
    }
}
