/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.labreport;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.RequestOptions;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.labreport.requests.CreateLabReportParserJobBody;
import com.junction.api.resources.labreport.requests.ParserGetJobLabReportRequest;
import com.junction.api.types.HttpValidationError;
import com.junction.api.types.ParsingJob;
import java.io.File;
import java.io.IOException;
import java.nio.file.Files;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawLabReportClient {
    protected final ClientOptions clientOptions;

    public AsyncRawLabReportClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Creates a parse job, uploads the file(s) to provider, persists the job row,
     * and starts the ParseLabReport. Returns a generated job_id.
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserCreateJob(
            File file, CreateLabReportParserJobBody request) {
        return parserCreateJob(file, request, null);
    }

    /**
     * Creates a parse job, uploads the file(s) to provider, persists the job row,
     * and starts the ParseLabReport. Returns a generated job_id.
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserCreateJob(
            File file, CreateLabReportParserJobBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lab_report/v1/parser/job");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        try {
            String fileMimeType = Files.probeContentType(file.toPath());
            MediaType fileMimeTypeMediaType = fileMimeType != null ? MediaType.parse(fileMimeType) : null;
            multipartBodyBuilder.addFormDataPart(
                    "file", file.getName(), RequestBody.create(file, fileMimeTypeMediaType));
            multipartBodyBuilder.addFormDataPart("user_id", request.getUserId());
            if (request.getNeedsHumanReview().isPresent()) {
                multipartBodyBuilder.addFormDataPart(
                        "needs_human_review",
                        request.getNeedsHumanReview().get().toString());
            }
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", multipartBodyBuilder.build())
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ParsingJob>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ParsingJob.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Retrieves the parse job status and stored result if completed.
     * <p>Returns:
     * ParseLabResultJobResponse with job status and parsed data (if complete)</p>
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserGetJob(String jobId) {
        return parserGetJob(jobId, ParserGetJobLabReportRequest.builder().build());
    }

    /**
     * Retrieves the parse job status and stored result if completed.
     * <p>Returns:
     * ParseLabResultJobResponse with job status and parsed data (if complete)</p>
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserGetJob(
            String jobId, RequestOptions requestOptions) {
        return parserGetJob(jobId, ParserGetJobLabReportRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves the parse job status and stored result if completed.
     * <p>Returns:
     * ParseLabResultJobResponse with job status and parsed data (if complete)</p>
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserGetJob(
            String jobId, ParserGetJobLabReportRequest request) {
        return parserGetJob(jobId, request, null);
    }

    /**
     * Retrieves the parse job status and stored result if completed.
     * <p>Returns:
     * ParseLabResultJobResponse with job status and parsed data (if complete)</p>
     */
    public CompletableFuture<JunctionHttpResponse<ParsingJob>> parserGetJob(
            String jobId, ParserGetJobLabReportRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("lab_report/v1/parser/job")
                .addPathSegment(jobId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ParsingJob>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ParsingJob.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
