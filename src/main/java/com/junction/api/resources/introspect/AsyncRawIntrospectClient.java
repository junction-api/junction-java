/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.introspect;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.QueryStringMapper;
import com.junction.api.core.RequestOptions;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.introspect.requests.GetUserHistoricalPullsIntrospectRequest;
import com.junction.api.resources.introspect.requests.GetUserResourcesIntrospectRequest;
import com.junction.api.types.HttpValidationError;
import com.junction.api.types.UserHistoricalPullsResponse;
import com.junction.api.types.UserResourcesResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawIntrospectClient {
    protected final ClientOptions clientOptions;

    public AsyncRawIntrospectClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public CompletableFuture<JunctionHttpResponse<UserResourcesResponse>> getUserResources() {
        return getUserResources(GetUserResourcesIntrospectRequest.builder().build());
    }

    public CompletableFuture<JunctionHttpResponse<UserResourcesResponse>> getUserResources(
            RequestOptions requestOptions) {
        return getUserResources(GetUserResourcesIntrospectRequest.builder().build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<UserResourcesResponse>> getUserResources(
            GetUserResourcesIntrospectRequest request) {
        return getUserResources(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<UserResourcesResponse>> getUserResources(
            GetUserResourcesIntrospectRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/introspect/resources");
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getProvider().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider", request.getProvider().get(), false);
        }
        if (request.getUserLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_limit", request.getUserLimit().get(), false);
        }
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getNextCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "next_cursor", request.getNextCursor().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<UserResourcesResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UserResourcesResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<UserHistoricalPullsResponse>> getUserHistoricalPulls() {
        return getUserHistoricalPulls(
                GetUserHistoricalPullsIntrospectRequest.builder().build());
    }

    public CompletableFuture<JunctionHttpResponse<UserHistoricalPullsResponse>> getUserHistoricalPulls(
            RequestOptions requestOptions) {
        return getUserHistoricalPulls(
                GetUserHistoricalPullsIntrospectRequest.builder().build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<UserHistoricalPullsResponse>> getUserHistoricalPulls(
            GetUserHistoricalPullsIntrospectRequest request) {
        return getUserHistoricalPulls(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<UserHistoricalPullsResponse>> getUserHistoricalPulls(
            GetUserHistoricalPullsIntrospectRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/introspect/historical_pull");
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getProvider().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider", request.getProvider().get(), false);
        }
        if (request.getUserLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_limit", request.getUserLimit().get(), false);
        }
        if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "cursor", request.getCursor().get(), false);
        }
        if (request.getNextCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "next_cursor", request.getNextCursor().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<UserHistoricalPullsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, UserHistoricalPullsResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
