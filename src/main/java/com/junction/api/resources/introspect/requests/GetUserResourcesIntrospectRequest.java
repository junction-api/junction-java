/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.introspect.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.junction.api.core.Nullable;
import com.junction.api.core.ObjectMappers;
import com.junction.api.types.Providers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GetUserResourcesIntrospectRequest.Builder.class)
public final class GetUserResourcesIntrospectRequest {
    private final Optional<String> userId;

    private final Optional<Providers> provider;

    private final Optional<Integer> userLimit;

    private final Optional<String> cursor;

    private final Optional<String> nextCursor;

    private final Map<String, Object> additionalProperties;

    private GetUserResourcesIntrospectRequest(
            Optional<String> userId,
            Optional<Providers> provider,
            Optional<Integer> userLimit,
            Optional<String> cursor,
            Optional<String> nextCursor,
            Map<String, Object> additionalProperties) {
        this.userId = userId;
        this.provider = provider;
        this.userLimit = userLimit;
        this.cursor = cursor;
        this.nextCursor = nextCursor;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Filter by user ID.
     */
    @JsonIgnore
    public Optional<String> getUserId() {
        if (userId == null) {
            return Optional.empty();
        }
        return userId;
    }

    @JsonIgnore
    public Optional<Providers> getProvider() {
        if (provider == null) {
            return Optional.empty();
        }
        return provider;
    }

    @JsonIgnore
    public Optional<Integer> getUserLimit() {
        if (userLimit == null) {
            return Optional.empty();
        }
        return userLimit;
    }

    @JsonIgnore
    public Optional<String> getCursor() {
        if (cursor == null) {
            return Optional.empty();
        }
        return cursor;
    }

    /**
     * @return The cursor for fetching the next page, or <code>null</code> to fetch the first page.
     */
    @JsonIgnore
    public Optional<String> getNextCursor() {
        if (nextCursor == null) {
            return Optional.empty();
        }
        return nextCursor;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof GetUserResourcesIntrospectRequest && equalTo((GetUserResourcesIntrospectRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(GetUserResourcesIntrospectRequest other) {
        return userId.equals(other.userId)
                && provider.equals(other.provider)
                && userLimit.equals(other.userLimit)
                && cursor.equals(other.cursor)
                && nextCursor.equals(other.nextCursor);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.userId, this.provider, this.userLimit, this.cursor, this.nextCursor);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<String> userId = Optional.empty();

        private Optional<Providers> provider = Optional.empty();

        private Optional<Integer> userLimit = Optional.empty();

        private Optional<String> cursor = Optional.empty();

        private Optional<String> nextCursor = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(GetUserResourcesIntrospectRequest other) {
            userId(other.getUserId());
            provider(other.getProvider());
            userLimit(other.getUserLimit());
            cursor(other.getCursor());
            nextCursor(other.getNextCursor());
            return this;
        }

        /**
         * <p>Filter by user ID.</p>
         */
        @JsonSetter(value = "user_id", nulls = Nulls.SKIP)
        public Builder userId(Optional<String> userId) {
            this.userId = userId;
            return this;
        }

        public Builder userId(String userId) {
            this.userId = Optional.ofNullable(userId);
            return this;
        }

        public Builder userId(Nullable<String> userId) {
            if (userId.isNull()) {
                this.userId = null;
            } else if (userId.isEmpty()) {
                this.userId = Optional.empty();
            } else {
                this.userId = Optional.of(userId.get());
            }
            return this;
        }

        @JsonSetter(value = "provider", nulls = Nulls.SKIP)
        public Builder provider(Optional<Providers> provider) {
            this.provider = provider;
            return this;
        }

        public Builder provider(Providers provider) {
            this.provider = Optional.ofNullable(provider);
            return this;
        }

        public Builder provider(Nullable<Providers> provider) {
            if (provider.isNull()) {
                this.provider = null;
            } else if (provider.isEmpty()) {
                this.provider = Optional.empty();
            } else {
                this.provider = Optional.of(provider.get());
            }
            return this;
        }

        @JsonSetter(value = "user_limit", nulls = Nulls.SKIP)
        public Builder userLimit(Optional<Integer> userLimit) {
            this.userLimit = userLimit;
            return this;
        }

        public Builder userLimit(Integer userLimit) {
            this.userLimit = Optional.ofNullable(userLimit);
            return this;
        }

        public Builder userLimit(Nullable<Integer> userLimit) {
            if (userLimit.isNull()) {
                this.userLimit = null;
            } else if (userLimit.isEmpty()) {
                this.userLimit = Optional.empty();
            } else {
                this.userLimit = Optional.of(userLimit.get());
            }
            return this;
        }

        @JsonSetter(value = "cursor", nulls = Nulls.SKIP)
        public Builder cursor(Optional<String> cursor) {
            this.cursor = cursor;
            return this;
        }

        public Builder cursor(String cursor) {
            this.cursor = Optional.ofNullable(cursor);
            return this;
        }

        public Builder cursor(Nullable<String> cursor) {
            if (cursor.isNull()) {
                this.cursor = null;
            } else if (cursor.isEmpty()) {
                this.cursor = Optional.empty();
            } else {
                this.cursor = Optional.of(cursor.get());
            }
            return this;
        }

        /**
         * <p>The cursor for fetching the next page, or <code>null</code> to fetch the first page.</p>
         */
        @JsonSetter(value = "next_cursor", nulls = Nulls.SKIP)
        public Builder nextCursor(Optional<String> nextCursor) {
            this.nextCursor = nextCursor;
            return this;
        }

        public Builder nextCursor(String nextCursor) {
            this.nextCursor = Optional.ofNullable(nextCursor);
            return this;
        }

        public Builder nextCursor(Nullable<String> nextCursor) {
            if (nextCursor.isNull()) {
                this.nextCursor = null;
            } else if (nextCursor.isEmpty()) {
                this.nextCursor = Optional.empty();
            } else {
                this.nextCursor = Optional.of(nextCursor.get());
            }
            return this;
        }

        public GetUserResourcesIntrospectRequest build() {
            return new GetUserResourcesIntrospectRequest(
                    userId, provider, userLimit, cursor, nextCursor, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
