/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.insurance;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.MediaTypes;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.QueryStringMapper;
import com.junction.api.core.RequestOptions;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.insurance.requests.PayorSearchRequest;
import com.junction.api.resources.insurance.requests.SearchDiagnosisInsuranceRequest;
import com.junction.api.resources.insurance.requests.SearchGetPayorInfoInsuranceRequest;
import com.junction.api.types.ClientFacingDiagnosisInformation;
import com.junction.api.types.ClientFacingPayorSearchResponse;
import com.junction.api.types.ClientFacingPayorSearchResponseDeprecated;
import com.junction.api.types.HttpValidationError;
import java.io.IOException;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawInsuranceClient {
    protected final ClientOptions clientOptions;

    public RawInsuranceClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponse>> searchGetPayorInfo() {
        return searchGetPayorInfo(SearchGetPayorInfoInsuranceRequest.builder().build());
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponse>> searchGetPayorInfo(
            RequestOptions requestOptions) {
        return searchGetPayorInfo(SearchGetPayorInfoInsuranceRequest.builder().build(), requestOptions);
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponse>> searchGetPayorInfo(
            SearchGetPayorInfoInsuranceRequest request) {
        return searchGetPayorInfo(request, null);
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponse>> searchGetPayorInfo(
            SearchGetPayorInfoInsuranceRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/insurance/search/payor");
        if (request.getInsuranceName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "insurance_name", request.getInsuranceName().get(), false);
        }
        if (request.getProvider().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider", request.getProvider().get(), false);
        }
        if (request.getProviderPayorId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider_payor_id", request.getProviderPayorId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<List<ClientFacingPayorSearchResponse>>() {}),
                        response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponseDeprecated>> searchPayorInfo() {
        return searchPayorInfo(PayorSearchRequest.builder().build());
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponseDeprecated>> searchPayorInfo(
            RequestOptions requestOptions) {
        return searchPayorInfo(PayorSearchRequest.builder().build(), requestOptions);
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponseDeprecated>> searchPayorInfo(
            PayorSearchRequest request) {
        return searchPayorInfo(request, null);
    }

    public JunctionHttpResponse<List<ClientFacingPayorSearchResponseDeprecated>> searchPayorInfo(
            PayorSearchRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/insurance/search/payor");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString,
                                new TypeReference<List<ClientFacingPayorSearchResponseDeprecated>>() {}),
                        response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<List<ClientFacingDiagnosisInformation>> searchDiagnosis(
            SearchDiagnosisInsuranceRequest request) {
        return searchDiagnosis(request, null);
    }

    public JunctionHttpResponse<List<ClientFacingDiagnosisInformation>> searchDiagnosis(
            SearchDiagnosisInsuranceRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/insurance/search/diagnosis");
        QueryStringMapper.addQueryParameter(httpUrl, "diagnosis_query", request.getDiagnosisQuery(), false);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<List<ClientFacingDiagnosisInformation>>() {}),
                        response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }
}
