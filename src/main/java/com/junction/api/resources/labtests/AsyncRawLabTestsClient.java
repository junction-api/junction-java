/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.labtests;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.MediaTypes;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.QueryStringMapper;
import com.junction.api.core.RequestOptions;
import com.junction.api.core.ResponseBodyInputStream;
import com.junction.api.errors.NotFoundError;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.labtests.requests.ApiApiV1EndpointsVitalApiLabTestingOrdersHelpersAppointmentCancelRequest;
import com.junction.api.resources.labtests.requests.BookPhlebotomyAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.BookPscAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.CancelOrderLabTestsRequest;
import com.junction.api.resources.labtests.requests.CreateLabTestRequest;
import com.junction.api.resources.labtests.requests.CreateOrderRequestCompatible;
import com.junction.api.resources.labtests.requests.GetAreaInfoLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetByIdLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetLabTestCollectionInstructionPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetLabelsPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetMarkersByLabAndProviderIdLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetMarkersForLabTestLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetMarkersForOrderSetLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetMarkersLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrderAbnPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrderCollectionInstructionPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrderLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrderPscInfoLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrderRequistionPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetOrdersLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPaginatedLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPhlebotomyAppointmentAvailabilityLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPhlebotomyAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPscAppointmentAvailabilityLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPscAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetPscInfoLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetResultMetadataLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetResultPdfLabTestsRequest;
import com.junction.api.resources.labtests.requests.GetResultRawLabTestsRequest;
import com.junction.api.resources.labtests.requests.ImportOrderBody;
import com.junction.api.resources.labtests.requests.RequestAppointmentRequest;
import com.junction.api.resources.labtests.requests.ReschedulePhlebotomyAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.ReschedulePscAppointmentLabTestsRequest;
import com.junction.api.resources.labtests.requests.SimulateOrderProcessLabTestsRequest;
import com.junction.api.resources.labtests.requests.UpdateLabTestRequest;
import com.junction.api.resources.labtests.requests.UpdateOnSiteCollectionOrderDrawCompletedLabTestsRequest;
import com.junction.api.resources.labtests.requests.UpdateOrderBody;
import com.junction.api.resources.labtests.requests.ValidateIcdCodesBody;
import com.junction.api.resources.labtests.requests.VitalCoreClientsLabTestGetlabsSchemaAppointmentCancelRequest;
import com.junction.api.types.AppointmentAvailabilitySlots;
import com.junction.api.types.AppointmentBookingRequest;
import com.junction.api.types.AreaInfo;
import com.junction.api.types.ClientFacingAppointment;
import com.junction.api.types.ClientFacingAppointmentCancellationReason;
import com.junction.api.types.ClientFacingLab;
import com.junction.api.types.ClientFacingLabTest;
import com.junction.api.types.ClientFacingMarker;
import com.junction.api.types.ClientFacingOrder;
import com.junction.api.types.GetMarkersResponse;
import com.junction.api.types.GetOrdersResponse;
import com.junction.api.types.HttpValidationError;
import com.junction.api.types.LabResultsMetadata;
import com.junction.api.types.LabResultsRaw;
import com.junction.api.types.LabTestResourcesResponse;
import com.junction.api.types.NotFoundErrorBody;
import com.junction.api.types.OrderSetRequest;
import com.junction.api.types.PostOrderResponse;
import com.junction.api.types.PscInfo;
import com.junction.api.types.UsAddress;
import com.junction.api.types.ValidateIcdCodesResponse;
import java.io.IOException;
import java.io.InputStream;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawLabTestsClient {
    protected final ClientOptions clientOptions;

    public AsyncRawLabTestsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLabTest>>> get() {
        return get(GetLabTestsRequest.builder().build());
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLabTest>>> get(RequestOptions requestOptions) {
        return get(GetLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLabTest>>> get(GetLabTestsRequest request) {
        return get(request, null);
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLabTest>>> get(
            GetLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests");
        if (request.getGenerationMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "generation_method", request.getGenerationMethod().get(), false);
        }
        if (request.getLabSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_slug", request.getLabSlug().get(), false);
        }
        if (request.getCollectionMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "collection_method", request.getCollectionMethod().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "name", request.getName().get(), false);
        }
        if (request.getOrderKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_key", request.getOrderKey().get(), false);
        }
        if (request.getOrderDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_direction", request.getOrderDirection().get(), false);
        }
        if (request.getMarkerIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "marker_ids", request.getMarkerIds().get(), true);
        }
        if (request.getProviderIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider_ids", request.getProviderIds().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<List<ClientFacingLabTest>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<ClientFacingLabTest>>() {}),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> create(CreateLabTestRequest request) {
        return create(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> create(
            CreateLabTestRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingLabTest.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> getById(String labTestId) {
        return getById(labTestId, GetByIdLabTestsRequest.builder().build());
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> getById(
            String labTestId, RequestOptions requestOptions) {
        return getById(labTestId, GetByIdLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> getById(
            String labTestId, GetByIdLabTestsRequest request) {
        return getById(labTestId, request, null);
    }

    /**
     * GET all the lab tests the team has access to.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> getById(
            String labTestId, GetByIdLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests")
                .addPathSegment(labTestId);
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingLabTest.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> updateLabTest(String labTestId) {
        return updateLabTest(labTestId, UpdateLabTestRequest.builder().build());
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> updateLabTest(
            String labTestId, RequestOptions requestOptions) {
        return updateLabTest(labTestId, UpdateLabTestRequest.builder().build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> updateLabTest(
            String labTestId, UpdateLabTestRequest request) {
        return updateLabTest(labTestId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> updateLabTest(
            String labTestId, UpdateLabTestRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests")
                .addPathSegment(labTestId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingLabTest>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingLabTest.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * List active and orderable markers for a given Lab. Note that reflex markers are not included.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkers() {
        return getMarkers(GetMarkersLabTestsRequest.builder().build());
    }

    /**
     * List active and orderable markers for a given Lab. Note that reflex markers are not included.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkers(RequestOptions requestOptions) {
        return getMarkers(GetMarkersLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * List active and orderable markers for a given Lab. Note that reflex markers are not included.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkers(GetMarkersLabTestsRequest request) {
        return getMarkers(request, null);
    }

    /**
     * List active and orderable markers for a given Lab. Note that reflex markers are not included.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkers(
            GetMarkersLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests/markers");
        if (request.getLabSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_slug", request.getLabSlug().get(), false);
        }
        if (request.getName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "name", request.getName().get(), false);
        }
        if (request.getALaCarteEnabled().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "a_la_carte_enabled", request.getALaCarteEnabled().get(), false);
        }
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "size", request.getSize().get(), false);
        }
        if (request.getLabId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_id", request.getLabId().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetMarkersResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForOrderSet(OrderSetRequest body) {
        return getMarkersForOrderSet(
                GetMarkersForOrderSetLabTestsRequest.builder().body(body).build());
    }

    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForOrderSet(
            OrderSetRequest body, RequestOptions requestOptions) {
        return getMarkersForOrderSet(
                GetMarkersForOrderSetLabTestsRequest.builder().body(body).build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForOrderSet(
            GetMarkersForOrderSetLabTestsRequest request) {
        return getMarkersForOrderSet(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForOrderSet(
            GetMarkersForOrderSetLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests/list_order_set_markers");
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "size", request.getSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetMarkersResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * List all markers for a given Lab Test, as well as any associated reflex markers.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForLabTest(String labTestId) {
        return getMarkersForLabTest(
                labTestId, GetMarkersForLabTestLabTestsRequest.builder().build());
    }

    /**
     * List all markers for a given Lab Test, as well as any associated reflex markers.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForLabTest(
            String labTestId, RequestOptions requestOptions) {
        return getMarkersForLabTest(
                labTestId, GetMarkersForLabTestLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * List all markers for a given Lab Test, as well as any associated reflex markers.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForLabTest(
            String labTestId, GetMarkersForLabTestLabTestsRequest request) {
        return getMarkersForLabTest(labTestId, request, null);
    }

    /**
     * List all markers for a given Lab Test, as well as any associated reflex markers.
     */
    public CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> getMarkersForLabTest(
            String labTestId, GetMarkersForLabTestLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests")
                .addPathSegment(labTestId)
                .addPathSegments("markers");
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "size", request.getSize().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<GetMarkersResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetMarkersResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET a specific marker for the given lab and provider_id
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingMarker>> getMarkersByLabAndProviderId(
            String providerId, int labId) {
        return getMarkersByLabAndProviderId(
                providerId,
                labId,
                GetMarkersByLabAndProviderIdLabTestsRequest.builder().build());
    }

    /**
     * GET a specific marker for the given lab and provider_id
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingMarker>> getMarkersByLabAndProviderId(
            String providerId, int labId, RequestOptions requestOptions) {
        return getMarkersByLabAndProviderId(
                providerId,
                labId,
                GetMarkersByLabAndProviderIdLabTestsRequest.builder().build(),
                requestOptions);
    }

    /**
     * GET a specific marker for the given lab and provider_id
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingMarker>> getMarkersByLabAndProviderId(
            String providerId, int labId, GetMarkersByLabAndProviderIdLabTestsRequest request) {
        return getMarkersByLabAndProviderId(providerId, labId, request, null);
    }

    /**
     * GET a specific marker for the given lab and provider_id
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingMarker>> getMarkersByLabAndProviderId(
            String providerId,
            int labId,
            GetMarkersByLabAndProviderIdLabTestsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests")
                .addPathSegment(Integer.toString(labId))
                .addPathSegments("markers")
                .addPathSegment(providerId);
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingMarker>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingMarker.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET all the labs.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLab>>> getLabs() {
        return getLabs(null);
    }

    /**
     * GET all the labs.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingLab>>> getLabs(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_tests/labs");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<List<ClientFacingLab>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, new TypeReference<List<ClientFacingLab>>() {}),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET lab tests the team has access to as a paginated list.
     */
    public CompletableFuture<JunctionHttpResponse<LabTestResourcesResponse>> getPaginated() {
        return getPaginated(GetPaginatedLabTestsRequest.builder().build());
    }

    /**
     * GET lab tests the team has access to as a paginated list.
     */
    public CompletableFuture<JunctionHttpResponse<LabTestResourcesResponse>> getPaginated(
            RequestOptions requestOptions) {
        return getPaginated(GetPaginatedLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET lab tests the team has access to as a paginated list.
     */
    public CompletableFuture<JunctionHttpResponse<LabTestResourcesResponse>> getPaginated(
            GetPaginatedLabTestsRequest request) {
        return getPaginated(request, null);
    }

    /**
     * GET lab tests the team has access to as a paginated list.
     */
    public CompletableFuture<JunctionHttpResponse<LabTestResourcesResponse>> getPaginated(
            GetPaginatedLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_test");
        if (request.getLabTestLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_test_limit", request.getLabTestLimit().get(), false);
        }
        if (request.getNextCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "next_cursor", request.getNextCursor().get(), false);
        }
        if (request.getGenerationMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "generation_method", request.getGenerationMethod().get(), false);
        }
        if (request.getLabSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_slug", request.getLabSlug().get(), false);
        }
        if (request.getCollectionMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "collection_method", request.getCollectionMethod().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "name", request.getName().get(), false);
        }
        if (request.getOrderKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_key", request.getOrderKey().get(), false);
        }
        if (request.getOrderDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_direction", request.getOrderDirection().get(), false);
        }
        if (request.getMarkerIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "marker_ids", request.getMarkerIds().get(), true);
        }
        if (request.getProviderIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "provider_ids", request.getProviderIds().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<LabTestResourcesResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LabTestResourcesResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabTestCollectionInstructionPdf(String labTestId) {
        return getLabTestCollectionInstructionPdf(
                labTestId,
                GetLabTestCollectionInstructionPdfLabTestsRequest.builder().build());
    }

    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabTestCollectionInstructionPdf(
            String labTestId, RequestOptions requestOptions) {
        return getLabTestCollectionInstructionPdf(
                labTestId,
                GetLabTestCollectionInstructionPdfLabTestsRequest.builder().build(),
                requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabTestCollectionInstructionPdf(
            String labTestId, GetLabTestCollectionInstructionPdfLabTestsRequest request) {
        return getLabTestCollectionInstructionPdf(labTestId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabTestCollectionInstructionPdf(
            String labTestId,
            GetLabTestCollectionInstructionPdfLabTestsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/lab_test")
                .addPathSegment(labTestId)
                .addPathSegments("collection_instruction_pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET many orders with filters.
     */
    public CompletableFuture<JunctionHttpResponse<GetOrdersResponse>> getOrders() {
        return getOrders(GetOrdersLabTestsRequest.builder().build());
    }

    /**
     * GET many orders with filters.
     */
    public CompletableFuture<JunctionHttpResponse<GetOrdersResponse>> getOrders(RequestOptions requestOptions) {
        return getOrders(GetOrdersLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET many orders with filters.
     */
    public CompletableFuture<JunctionHttpResponse<GetOrdersResponse>> getOrders(GetOrdersLabTestsRequest request) {
        return getOrders(request, null);
    }

    /**
     * GET many orders with filters.
     */
    public CompletableFuture<JunctionHttpResponse<GetOrdersResponse>> getOrders(
            GetOrdersLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/orders");
        if (request.getSearchInput().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "search_input", request.getSearchInput().get(), false);
        }
        if (request.getStartDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "start_date", request.getStartDate().get(), false);
        }
        if (request.getEndDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "end_date", request.getEndDate().get(), false);
        }
        if (request.getUpdatedStartDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "updated_start_date", request.getUpdatedStartDate().get(), false);
        }
        if (request.getUpdatedEndDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "updated_end_date", request.getUpdatedEndDate().get(), false);
        }
        if (request.getOrderKey().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_key", request.getOrderKey().get(), false);
        }
        if (request.getOrderDirection().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_direction", request.getOrderDirection().get(), false);
        }
        if (request.getIsCritical().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "is_critical", request.getIsCritical().get(), false);
        }
        if (request.getInterpretation().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "interpretation", request.getInterpretation().get(), false);
        }
        if (request.getUserId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "user_id", request.getUserId().get(), false);
        }
        if (request.getPatientName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "patient_name", request.getPatientName().get(), false);
        }
        if (request.getShippingRecipientName().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "shipping_recipient_name",
                    request.getShippingRecipientName().get(),
                    false);
        }
        if (request.getOrderTransactionId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "order_transaction_id",
                    request.getOrderTransactionId().get(),
                    false);
        }
        if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "page", request.getPage().get(), false);
        }
        if (request.getSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "size", request.getSize().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), true);
        }
        if (request.getOrderType().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_type", request.getOrderType().get(), true);
        }
        if (request.getOrderActivationTypes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "order_activation_types",
                    request.getOrderActivationTypes().get(),
                    true);
        }
        if (request.getOrderIds().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "order_ids", request.getOrderIds().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<GetOrdersResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetOrdersResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Return the available time slots to book an appointment with a phlebotomist
     * for the given address and order.
     */
    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPhlebotomyAppointmentAvailability(
            UsAddress body) {
        return getPhlebotomyAppointmentAvailability(GetPhlebotomyAppointmentAvailabilityLabTestsRequest.builder()
                .body(body)
                .build());
    }

    /**
     * Return the available time slots to book an appointment with a phlebotomist
     * for the given address and order.
     */
    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPhlebotomyAppointmentAvailability(
            UsAddress body, RequestOptions requestOptions) {
        return getPhlebotomyAppointmentAvailability(
                GetPhlebotomyAppointmentAvailabilityLabTestsRequest.builder()
                        .body(body)
                        .build(),
                requestOptions);
    }

    /**
     * Return the available time slots to book an appointment with a phlebotomist
     * for the given address and order.
     */
    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPhlebotomyAppointmentAvailability(
            GetPhlebotomyAppointmentAvailabilityLabTestsRequest request) {
        return getPhlebotomyAppointmentAvailability(request, null);
    }

    /**
     * Return the available time slots to book an appointment with a phlebotomist
     * for the given address and order.
     */
    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPhlebotomyAppointmentAvailability(
            GetPhlebotomyAppointmentAvailabilityLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/phlebotomy/appointment/availability");
        if (request.getStartDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "start_date", request.getStartDate().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, AppointmentAvailabilitySlots.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Book an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPhlebotomyAppointment(
            String orderId, BookPhlebotomyAppointmentLabTestsRequest request) {
        return bookPhlebotomyAppointment(orderId, request, null);
    }

    /**
     * Book an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPhlebotomyAppointment(
            String orderId, BookPhlebotomyAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("phlebotomy/appointment")
                .addPathSegments("book");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Request an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> requestPhlebotomyAppointment(
            String orderId, RequestAppointmentRequest request) {
        return requestPhlebotomyAppointment(orderId, request, null);
    }

    /**
     * Request an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> requestPhlebotomyAppointment(
            String orderId, RequestAppointmentRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("phlebotomy/appointment")
                .addPathSegments("request");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Reschedule a previously booked at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> reschedulePhlebotomyAppointment(
            String orderId, ReschedulePhlebotomyAppointmentLabTestsRequest request) {
        return reschedulePhlebotomyAppointment(orderId, request, null);
    }

    /**
     * Reschedule a previously booked at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> reschedulePhlebotomyAppointment(
            String orderId, ReschedulePhlebotomyAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("phlebotomy/appointment")
                .addPathSegments("reschedule");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Cancel a previously booked at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> cancelPhlebotomyAppointment(
            String orderId, ApiApiV1EndpointsVitalApiLabTestingOrdersHelpersAppointmentCancelRequest request) {
        return cancelPhlebotomyAppointment(orderId, request, null);
    }

    /**
     * Cancel a previously booked at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> cancelPhlebotomyAppointment(
            String orderId,
            ApiApiV1EndpointsVitalApiLabTestingOrdersHelpersAppointmentCancelRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("phlebotomy/appointment")
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Get the list of reasons for cancelling an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>>
            getPhlebotomyAppointmentCancellationReason() {
        return getPhlebotomyAppointmentCancellationReason(null);
    }

    /**
     * Get the list of reasons for cancelling an at-home phlebotomy appointment.
     */
    public CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>>
            getPhlebotomyAppointmentCancellationReason(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/phlebotomy/appointment/cancellation-reasons");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString,
                                        new TypeReference<List<ClientFacingAppointmentCancellationReason>>() {}),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPhlebotomyAppointment(String orderId) {
        return getPhlebotomyAppointment(
                orderId, GetPhlebotomyAppointmentLabTestsRequest.builder().build());
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPhlebotomyAppointment(
            String orderId, RequestOptions requestOptions) {
        return getPhlebotomyAppointment(
                orderId, GetPhlebotomyAppointmentLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPhlebotomyAppointment(
            String orderId, GetPhlebotomyAppointmentLabTestsRequest request) {
        return getPhlebotomyAppointment(orderId, request, null);
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPhlebotomyAppointment(
            String orderId, GetPhlebotomyAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("phlebotomy")
                .addPathSegments("appointment");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET information about an area with respect to lab-testing.
     * <p>Information returned:</p>
     * <ul>
     * <li>Whether a given zip code is served by our Phlebotomy network.</li>
     * <li>List of Lab locations in the area.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<AreaInfo>> getAreaInfo(GetAreaInfoLabTestsRequest request) {
        return getAreaInfo(request, null);
    }

    /**
     * GET information about an area with respect to lab-testing.
     * <p>Information returned:</p>
     * <ul>
     * <li>Whether a given zip code is served by our Phlebotomy network.</li>
     * <li>List of Lab locations in the area.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<AreaInfo>> getAreaInfo(
            GetAreaInfoLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/area/info");
        QueryStringMapper.addQueryParameter(httpUrl, "zip_code", request.getZipCode(), false);
        if (request.getRadius().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "radius", request.getRadius().get(), false);
        }
        if (request.getLab().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "lab", request.getLab().get(), false);
        }
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (request.getLabs().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "labs", request.getLabs().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<AreaInfo>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AreaInfo.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getPscInfo(GetPscInfoLabTestsRequest request) {
        return getPscInfo(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getPscInfo(
            GetPscInfoLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/psc/info");
        QueryStringMapper.addQueryParameter(httpUrl, "zip_code", request.getZipCode(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "lab_id", request.getLabId(), false);
        if (request.getRadius().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "radius", request.getRadius().get(), false);
        }
        if (request.getLabAccountId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "lab_account_id", request.getLabAccountId().get(), false);
        }
        if (request.getCapabilities().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "capabilities", request.getCapabilities().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PscInfo>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PscInfo.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getOrderPscInfo(String orderId) {
        return getOrderPscInfo(orderId, GetOrderPscInfoLabTestsRequest.builder().build());
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getOrderPscInfo(
            String orderId, RequestOptions requestOptions) {
        return getOrderPscInfo(orderId, GetOrderPscInfoLabTestsRequest.builder().build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getOrderPscInfo(
            String orderId, GetOrderPscInfoLabTestsRequest request) {
        return getOrderPscInfo(orderId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<PscInfo>> getOrderPscInfo(
            String orderId, GetOrderPscInfoLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("psc")
                .addPathSegments("info");
        if (request.getRadius().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "radius", request.getRadius().get(), false);
        }
        if (request.getCapabilities().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "capabilities", request.getCapabilities().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PscInfo>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PscInfo.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * This endpoint returns the lab results for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getResultPdf(String orderId) {
        return getResultPdf(orderId, GetResultPdfLabTestsRequest.builder().build());
    }

    /**
     * This endpoint returns the lab results for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getResultPdf(
            String orderId, RequestOptions requestOptions) {
        return getResultPdf(orderId, GetResultPdfLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * This endpoint returns the lab results for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getResultPdf(
            String orderId, GetResultPdfLabTestsRequest request) {
        return getResultPdf(orderId, request, null);
    }

    /**
     * This endpoint returns the lab results for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getResultPdf(
            String orderId, GetResultPdfLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("result")
                .addPathSegments("pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Return metadata related to order results, such as lab metadata,
     * provider and sample dates.
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsMetadata>> getResultMetadata(String orderId) {
        return getResultMetadata(
                orderId, GetResultMetadataLabTestsRequest.builder().build());
    }

    /**
     * Return metadata related to order results, such as lab metadata,
     * provider and sample dates.
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsMetadata>> getResultMetadata(
            String orderId, RequestOptions requestOptions) {
        return getResultMetadata(
                orderId, GetResultMetadataLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * Return metadata related to order results, such as lab metadata,
     * provider and sample dates.
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsMetadata>> getResultMetadata(
            String orderId, GetResultMetadataLabTestsRequest request) {
        return getResultMetadata(orderId, request, null);
    }

    /**
     * Return metadata related to order results, such as lab metadata,
     * provider and sample dates.
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsMetadata>> getResultMetadata(
            String orderId, GetResultMetadataLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("result")
                .addPathSegments("metadata");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<LabResultsMetadata>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LabResultsMetadata.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Return both metadata and raw json test data
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsRaw>> getResultRaw(String orderId) {
        return getResultRaw(orderId, GetResultRawLabTestsRequest.builder().build());
    }

    /**
     * Return both metadata and raw json test data
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsRaw>> getResultRaw(
            String orderId, RequestOptions requestOptions) {
        return getResultRaw(orderId, GetResultRawLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * Return both metadata and raw json test data
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsRaw>> getResultRaw(
            String orderId, GetResultRawLabTestsRequest request) {
        return getResultRaw(orderId, request, null);
    }

    /**
     * Return both metadata and raw json test data
     */
    public CompletableFuture<JunctionHttpResponse<LabResultsRaw>> getResultRaw(
            String orderId, GetResultRawLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("result");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<LabResultsRaw>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LabResultsRaw.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * This endpoint returns the printed labels for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabelsPdf(
            String orderId, GetLabelsPdfLabTestsRequest request) {
        return getLabelsPdf(orderId, request, null);
    }

    /**
     * This endpoint returns the printed labels for the order.
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getLabelsPdf(
            String orderId, GetLabelsPdfLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("labels")
                .addPathSegments("pdf");
        if (request.getNumberOfLabels().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "number_of_labels", request.getNumberOfLabels().get(), false);
        }
        QueryStringMapper.addQueryParameter(httpUrl, "collection_date", request.getCollectionDate(), false);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPscAppointmentAvailability(
            GetPscAppointmentAvailabilityLabTestsRequest request) {
        return getPscAppointmentAvailability(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> getPscAppointmentAvailability(
            GetPscAppointmentAvailabilityLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/psc/appointment/availability");
        QueryStringMapper.addQueryParameter(httpUrl, "lab", request.getLab(), false);
        if (request.getStartDate().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "start_date", request.getStartDate().get(), false);
        }
        if (request.getZipCode().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "zip_code", request.getZipCode().get(), false);
        }
        if (request.getRadius().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "radius", request.getRadius().get(), false);
        }
        if (request.getAllowStale().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "allow_stale", request.getAllowStale().get(), false);
        }
        if (request.getSiteCodes().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "site_codes", request.getSiteCodes().get(), true);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<AppointmentAvailabilitySlots>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString, AppointmentAvailabilitySlots.class),
                                response));
                        return;
                    }
                    try {
                        switch (response.code()) {
                            case 404:
                                future.completeExceptionally(new NotFoundError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, NotFoundErrorBody.class),
                                        response));
                                return;
                            case 422:
                                future.completeExceptionally(new UnprocessableEntityError(
                                        ObjectMappers.JSON_MAPPER.readValue(
                                                responseBodyString, HttpValidationError.class),
                                        response));
                                return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPscAppointment(
            String orderId, AppointmentBookingRequest body) {
        return bookPscAppointment(
                orderId, BookPscAppointmentLabTestsRequest.builder().body(body).build());
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPscAppointment(
            String orderId, AppointmentBookingRequest body, RequestOptions requestOptions) {
        return bookPscAppointment(
                orderId, BookPscAppointmentLabTestsRequest.builder().body(body).build(), requestOptions);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPscAppointment(
            String orderId, BookPscAppointmentLabTestsRequest request) {
        return bookPscAppointment(orderId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> bookPscAppointment(
            String orderId, BookPscAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("psc/appointment")
                .addPathSegments("book");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "x-idempotency-key", request.getIdempotencyKey().get());
        }
        if (request.getIdempotencyError().isPresent()) {
            _requestBuilder.addHeader(
                    "x-idempotency-error", request.getIdempotencyError().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> reschedulePscAppointment(
            String orderId, ReschedulePscAppointmentLabTestsRequest request) {
        return reschedulePscAppointment(orderId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> reschedulePscAppointment(
            String orderId, ReschedulePscAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("psc/appointment")
                .addPathSegments("reschedule");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> cancelPscAppointment(
            String orderId, VitalCoreClientsLabTestGetlabsSchemaAppointmentCancelRequest request) {
        return cancelPscAppointment(orderId, request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> cancelPscAppointment(
            String orderId,
            VitalCoreClientsLabTestGetlabsSchemaAppointmentCancelRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("psc/appointment")
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>>
            getPscAppointmentCancellationReason() {
        return getPscAppointmentCancellationReason(null);
    }

    public CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>>
            getPscAppointmentCancellationReason(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/psc/appointment/cancellation-reasons");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<List<ClientFacingAppointmentCancellationReason>>> future =
                new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(
                                        responseBodyString,
                                        new TypeReference<List<ClientFacingAppointmentCancellationReason>>() {}),
                                response));
                        return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPscAppointment(String orderId) {
        return getPscAppointment(
                orderId, GetPscAppointmentLabTestsRequest.builder().build());
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPscAppointment(
            String orderId, RequestOptions requestOptions) {
        return getPscAppointment(
                orderId, GetPscAppointmentLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPscAppointment(
            String orderId, GetPscAppointmentLabTestsRequest request) {
        return getPscAppointment(orderId, request, null);
    }

    /**
     * Get the appointment associated with an order.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> getPscAppointment(
            String orderId, GetPscAppointmentLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("psc")
                .addPathSegments("appointment");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingAppointment>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingAppointment.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET collection instructions for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderCollectionInstructionPdf(String orderId) {
        return getOrderCollectionInstructionPdf(
                orderId,
                GetOrderCollectionInstructionPdfLabTestsRequest.builder().build());
    }

    /**
     * GET collection instructions for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderCollectionInstructionPdf(
            String orderId, RequestOptions requestOptions) {
        return getOrderCollectionInstructionPdf(
                orderId,
                GetOrderCollectionInstructionPdfLabTestsRequest.builder().build(),
                requestOptions);
    }

    /**
     * GET collection instructions for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderCollectionInstructionPdf(
            String orderId, GetOrderCollectionInstructionPdfLabTestsRequest request) {
        return getOrderCollectionInstructionPdf(orderId, request, null);
    }

    /**
     * GET collection instructions for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderCollectionInstructionPdf(
            String orderId, GetOrderCollectionInstructionPdfLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("collection_instruction_pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET requisition pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderRequistionPdf(String orderId) {
        return getOrderRequistionPdf(
                orderId, GetOrderRequistionPdfLabTestsRequest.builder().build());
    }

    /**
     * GET requisition pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderRequistionPdf(
            String orderId, RequestOptions requestOptions) {
        return getOrderRequistionPdf(
                orderId, GetOrderRequistionPdfLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET requisition pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderRequistionPdf(
            String orderId, GetOrderRequistionPdfLabTestsRequest request) {
        return getOrderRequistionPdf(orderId, request, null);
    }

    /**
     * GET requisition pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderRequistionPdf(
            String orderId, GetOrderRequistionPdfLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("requisition")
                .addPathSegments("pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET ABN pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderAbnPdf(String orderId) {
        return getOrderAbnPdf(orderId, GetOrderAbnPdfLabTestsRequest.builder().build());
    }

    /**
     * GET ABN pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderAbnPdf(
            String orderId, RequestOptions requestOptions) {
        return getOrderAbnPdf(orderId, GetOrderAbnPdfLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET ABN pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderAbnPdf(
            String orderId, GetOrderAbnPdfLabTestsRequest request) {
        return getOrderAbnPdf(orderId, request, null);
    }

    /**
     * GET ABN pdf for an order
     */
    public CompletableFuture<JunctionHttpResponse<InputStream>> getOrderAbnPdf(
            String orderId, GetOrderAbnPdfLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("abn_pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<InputStream>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try {
                    ResponseBody responseBody = response.body();
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * GET individual order by ID.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingOrder>> getOrder(String orderId) {
        return getOrder(orderId, GetOrderLabTestsRequest.builder().build());
    }

    /**
     * GET individual order by ID.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingOrder>> getOrder(
            String orderId, RequestOptions requestOptions) {
        return getOrder(orderId, GetOrderLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * GET individual order by ID.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingOrder>> getOrder(
            String orderId, GetOrderLabTestsRequest request) {
        return getOrder(orderId, request, null);
    }

    /**
     * GET individual order by ID.
     */
    public CompletableFuture<JunctionHttpResponse<ClientFacingOrder>> getOrder(
            String orderId, GetOrderLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ClientFacingOrder>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ClientFacingOrder.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Update a modifiable order's scheduled activation date.
     * <p>The order must be in <code>ordered</code> or <code>awaiting_registration</code> status. Setting
     * <code>activate_by</code> to a future date reschedules dispatch; setting it to <code>null</code>
     * clears the schedule and enqueues immediate dispatch for <code>ordered</code> orders.</p>
     * <p>Returns 400 when:</p>
     * <ul>
     * <li>the order is not in a modifiable status,</li>
     * <li>the order was created for immediate processing (cannot be scheduled
     * after the fact),</li>
     * <li><code>activate_by</code> is in the past.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOrder(String orderId) {
        return updateOrder(orderId, UpdateOrderBody.builder().build());
    }

    /**
     * Update a modifiable order's scheduled activation date.
     * <p>The order must be in <code>ordered</code> or <code>awaiting_registration</code> status. Setting
     * <code>activate_by</code> to a future date reschedules dispatch; setting it to <code>null</code>
     * clears the schedule and enqueues immediate dispatch for <code>ordered</code> orders.</p>
     * <p>Returns 400 when:</p>
     * <ul>
     * <li>the order is not in a modifiable status,</li>
     * <li>the order was created for immediate processing (cannot be scheduled
     * after the fact),</li>
     * <li><code>activate_by</code> is in the past.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOrder(
            String orderId, RequestOptions requestOptions) {
        return updateOrder(orderId, UpdateOrderBody.builder().build(), requestOptions);
    }

    /**
     * Update a modifiable order's scheduled activation date.
     * <p>The order must be in <code>ordered</code> or <code>awaiting_registration</code> status. Setting
     * <code>activate_by</code> to a future date reschedules dispatch; setting it to <code>null</code>
     * clears the schedule and enqueues immediate dispatch for <code>ordered</code> orders.</p>
     * <p>Returns 400 when:</p>
     * <ul>
     * <li>the order is not in a modifiable status,</li>
     * <li>the order was created for immediate processing (cannot be scheduled
     * after the fact),</li>
     * <li><code>activate_by</code> is in the past.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOrder(
            String orderId, UpdateOrderBody request) {
        return updateOrder(orderId, request, null);
    }

    /**
     * Update a modifiable order's scheduled activation date.
     * <p>The order must be in <code>ordered</code> or <code>awaiting_registration</code> status. Setting
     * <code>activate_by</code> to a future date reschedules dispatch; setting it to <code>null</code>
     * clears the schedule and enqueues immediate dispatch for <code>ordered</code> orders.</p>
     * <p>Returns 400 when:</p>
     * <ul>
     * <li>the order is not in a modifiable status,</li>
     * <li>the order was created for immediate processing (cannot be scheduled
     * after the fact),</li>
     * <li><code>activate_by</code> is in the past.</li>
     * </ul>
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOrder(
            String orderId, UpdateOrderBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PostOrderResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostOrderResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> createOrder(
            CreateOrderRequestCompatible request) {
        return createOrder(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> createOrder(
            CreateOrderRequestCompatible request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        if (request.getIdempotencyError().isPresent()) {
            _requestBuilder.addHeader(
                    "X-Idempotency-Error", request.getIdempotencyError().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PostOrderResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostOrderResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> importOrder(ImportOrderBody request) {
        return importOrder(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> importOrder(
            ImportOrderBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order/import");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PostOrderResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostOrderResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * POST cancel order
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> cancelOrder(String orderId) {
        return cancelOrder(orderId, CancelOrderLabTestsRequest.builder().build());
    }

    /**
     * POST cancel order
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> cancelOrder(
            String orderId, RequestOptions requestOptions) {
        return cancelOrder(orderId, CancelOrderLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * POST cancel order
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> cancelOrder(
            String orderId, CancelOrderLabTestsRequest request) {
        return cancelOrder(orderId, request, null);
    }

    /**
     * POST cancel order
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> cancelOrder(
            String orderId, CancelOrderLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("cancel");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PostOrderResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostOrderResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * Get available test kits.
     */
    public CompletableFuture<JunctionHttpResponse<Object>> simulateOrderProcess(String orderId) {
        return simulateOrderProcess(
                orderId, SimulateOrderProcessLabTestsRequest.builder().build());
    }

    /**
     * Get available test kits.
     */
    public CompletableFuture<JunctionHttpResponse<Object>> simulateOrderProcess(
            String orderId, RequestOptions requestOptions) {
        return simulateOrderProcess(
                orderId, SimulateOrderProcessLabTestsRequest.builder().build(), requestOptions);
    }

    /**
     * Get available test kits.
     */
    public CompletableFuture<JunctionHttpResponse<Object>> simulateOrderProcess(
            String orderId, SimulateOrderProcessLabTestsRequest request) {
        return simulateOrderProcess(orderId, request, null);
    }

    /**
     * Get available test kits.
     */
    public CompletableFuture<JunctionHttpResponse<Object>> simulateOrderProcess(
            String orderId, SimulateOrderProcessLabTestsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("test");
        if (request.getFinalStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "final_status", request.getFinalStatus().get(), false);
        }
        if (request.getDelay().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "delay", request.getDelay().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create("", null);
            if (request.getBody().isPresent()) {
                body = RequestBody.create(
                        ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
            }
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<Object>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    /**
     * PATCH update on site collection order when draw is completed
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOnSiteCollectionOrderDrawCompleted(
            String orderId) {
        return updateOnSiteCollectionOrderDrawCompleted(
                orderId,
                UpdateOnSiteCollectionOrderDrawCompletedLabTestsRequest.builder()
                        .build());
    }

    /**
     * PATCH update on site collection order when draw is completed
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOnSiteCollectionOrderDrawCompleted(
            String orderId, RequestOptions requestOptions) {
        return updateOnSiteCollectionOrderDrawCompleted(
                orderId,
                UpdateOnSiteCollectionOrderDrawCompletedLabTestsRequest.builder()
                        .build(),
                requestOptions);
    }

    /**
     * PATCH update on site collection order when draw is completed
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOnSiteCollectionOrderDrawCompleted(
            String orderId, UpdateOnSiteCollectionOrderDrawCompletedLabTestsRequest request) {
        return updateOnSiteCollectionOrderDrawCompleted(orderId, request, null);
    }

    /**
     * PATCH update on site collection order when draw is completed
     */
    public CompletableFuture<JunctionHttpResponse<PostOrderResponse>> updateOnSiteCollectionOrderDrawCompleted(
            String orderId,
            UpdateOnSiteCollectionOrderDrawCompletedLabTestsRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order")
                .addPathSegment(orderId)
                .addPathSegments("draw_completed");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<PostOrderResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostOrderResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }

    public CompletableFuture<JunctionHttpResponse<ValidateIcdCodesResponse>> validateIcdCodes(
            ValidateIcdCodesBody request) {
        return validateIcdCodes(request, null);
    }

    public CompletableFuture<JunctionHttpResponse<ValidateIcdCodesResponse>> validateIcdCodes(
            ValidateIcdCodesBody request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/insurance/validate_icd_codes");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new JunctionException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<JunctionHttpResponse<ValidateIcdCodesResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                        future.complete(new JunctionHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ValidateIcdCodesResponse.class),
                                response));
                        return;
                    }
                    try {
                        if (response.code() == 422) {
                            future.completeExceptionally(new UnprocessableEntityError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                                    response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new ApiError(
                            "Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new JunctionException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
