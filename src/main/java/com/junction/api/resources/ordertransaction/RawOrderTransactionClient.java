/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.resources.ordertransaction;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.junction.api.core.ApiError;
import com.junction.api.core.ClientOptions;
import com.junction.api.core.JunctionException;
import com.junction.api.core.JunctionHttpResponse;
import com.junction.api.core.ObjectMappers;
import com.junction.api.core.RequestOptions;
import com.junction.api.core.ResponseBodyInputStream;
import com.junction.api.errors.UnprocessableEntityError;
import com.junction.api.resources.ordertransaction.requests.GetTransactionOrderTransactionRequest;
import com.junction.api.resources.ordertransaction.requests.GetTransactionResultOrderTransactionRequest;
import com.junction.api.resources.ordertransaction.requests.GetTransactionResultPdfOrderTransactionRequest;
import com.junction.api.types.GetOrderTransactionResponse;
import com.junction.api.types.HttpValidationError;
import com.junction.api.types.LabResultsRaw;
import java.io.IOException;
import java.io.InputStream;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawOrderTransactionClient {
    protected final ClientOptions clientOptions;

    public RawOrderTransactionClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public JunctionHttpResponse<GetOrderTransactionResponse> getTransaction(String transactionId) {
        return getTransaction(
                transactionId, GetTransactionOrderTransactionRequest.builder().build());
    }

    public JunctionHttpResponse<GetOrderTransactionResponse> getTransaction(
            String transactionId, RequestOptions requestOptions) {
        return getTransaction(
                transactionId, GetTransactionOrderTransactionRequest.builder().build(), requestOptions);
    }

    public JunctionHttpResponse<GetOrderTransactionResponse> getTransaction(
            String transactionId, GetTransactionOrderTransactionRequest request) {
        return getTransaction(transactionId, request, null);
    }

    public JunctionHttpResponse<GetOrderTransactionResponse> getTransaction(
            String transactionId, GetTransactionOrderTransactionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order_transaction")
                .addPathSegment(transactionId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetOrderTransactionResponse.class),
                        response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<LabResultsRaw> getTransactionResult(String transactionId) {
        return getTransactionResult(
                transactionId,
                GetTransactionResultOrderTransactionRequest.builder().build());
    }

    public JunctionHttpResponse<LabResultsRaw> getTransactionResult(
            String transactionId, RequestOptions requestOptions) {
        return getTransactionResult(
                transactionId,
                GetTransactionResultOrderTransactionRequest.builder().build(),
                requestOptions);
    }

    public JunctionHttpResponse<LabResultsRaw> getTransactionResult(
            String transactionId, GetTransactionResultOrderTransactionRequest request) {
        return getTransactionResult(transactionId, request, null);
    }

    public JunctionHttpResponse<LabResultsRaw> getTransactionResult(
            String transactionId, GetTransactionResultOrderTransactionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order_transaction")
                .addPathSegment(transactionId)
                .addPathSegments("result");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LabResultsRaw.class), response);
            }
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }

    public JunctionHttpResponse<InputStream> getTransactionResultPdf(String transactionId) {
        return getTransactionResultPdf(
                transactionId,
                GetTransactionResultPdfOrderTransactionRequest.builder().build());
    }

    public JunctionHttpResponse<InputStream> getTransactionResultPdf(
            String transactionId, RequestOptions requestOptions) {
        return getTransactionResultPdf(
                transactionId,
                GetTransactionResultPdfOrderTransactionRequest.builder().build(),
                requestOptions);
    }

    public JunctionHttpResponse<InputStream> getTransactionResultPdf(
            String transactionId, GetTransactionResultPdfOrderTransactionRequest request) {
        return getTransactionResultPdf(transactionId, request, null);
    }

    public JunctionHttpResponse<InputStream> getTransactionResultPdf(
            String transactionId,
            GetTransactionResultPdfOrderTransactionRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v3/order_transaction")
                .addPathSegment(transactionId)
                .addPathSegments("result")
                .addPathSegments("pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try {
            Response response = client.newCall(okhttpRequest).execute();
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new JunctionHttpResponse<>(new ResponseBodyInputStream(response), response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                if (response.code() == 422) {
                    throw new UnprocessableEntityError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, HttpValidationError.class),
                            response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ApiError("Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (IOException e) {
            throw new JunctionException("Network error executing HTTP request", e);
        }
    }
}
