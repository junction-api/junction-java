/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class OrderTransactionStatus {
    public static final OrderTransactionStatus COMPLETED = new OrderTransactionStatus(Value.COMPLETED, "completed");

    public static final OrderTransactionStatus CANCELLED = new OrderTransactionStatus(Value.CANCELLED, "cancelled");

    public static final OrderTransactionStatus ACTIVE = new OrderTransactionStatus(Value.ACTIVE, "active");

    private final Value value;

    private final String string;

    OrderTransactionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof OrderTransactionStatus
                        && this.string.equals(((OrderTransactionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case COMPLETED:
                return visitor.visitCompleted();
            case CANCELLED:
                return visitor.visitCancelled();
            case ACTIVE:
                return visitor.visitActive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static OrderTransactionStatus valueOf(String value) {
        switch (value) {
            case "completed":
                return COMPLETED;
            case "cancelled":
                return CANCELLED;
            case "active":
                return ACTIVE;
            default:
                return new OrderTransactionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        COMPLETED,

        CANCELLED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitCompleted();

        T visitCancelled();

        T visitUnknown(String unknownType);
    }
}
