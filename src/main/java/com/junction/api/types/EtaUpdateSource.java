/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class EtaUpdateSource {
    public static final EtaUpdateSource LAB = new EtaUpdateSource(Value.LAB, "lab");

    public static final EtaUpdateSource PATIENT = new EtaUpdateSource(Value.PATIENT, "patient");

    public static final EtaUpdateSource JUNCTION = new EtaUpdateSource(Value.JUNCTION, "junction");

    private final Value value;

    private final String string;

    EtaUpdateSource(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof EtaUpdateSource && this.string.equals(((EtaUpdateSource) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case LAB:
                return visitor.visitLab();
            case PATIENT:
                return visitor.visitPatient();
            case JUNCTION:
                return visitor.visitJunction();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static EtaUpdateSource valueOf(String value) {
        switch (value) {
            case "lab":
                return LAB;
            case "patient":
                return PATIENT;
            case "junction":
                return JUNCTION;
            default:
                return new EtaUpdateSource(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        LAB,

        JUNCTION,

        PATIENT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitLab();

        T visitJunction();

        T visitPatient();

        T visitUnknown(String unknownType);
    }
}
