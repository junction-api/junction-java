/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class SleepType {
    public static final SleepType SHORT_SLEEP = new SleepType(Value.SHORT_SLEEP, "short_sleep");

    public static final SleepType UNKNOWN = new SleepType(Value.UNKNOWN, "unknown");

    public static final SleepType LONG_SLEEP = new SleepType(Value.LONG_SLEEP, "long_sleep");

    public static final SleepType ACKNOWLEDGED_NAP = new SleepType(Value.ACKNOWLEDGED_NAP, "acknowledged_nap");

    private final Value value;

    private final String string;

    SleepType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof SleepType && this.string.equals(((SleepType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SHORT_SLEEP:
                return visitor.visitShortSleep();
            case UNKNOWN:
                return visitor.visitUnknown();
            case LONG_SLEEP:
                return visitor.visitLongSleep();
            case ACKNOWLEDGED_NAP:
                return visitor.visitAcknowledgedNap();
            case _UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static SleepType valueOf(String value) {
        switch (value) {
            case "short_sleep":
                return SHORT_SLEEP;
            case "unknown":
                return UNKNOWN;
            case "long_sleep":
                return LONG_SLEEP;
            case "acknowledged_nap":
                return ACKNOWLEDGED_NAP;
            default:
                return new SleepType(Value._UNKNOWN, value);
        }
    }

    public enum Value {
        LONG_SLEEP,

        SHORT_SLEEP,

        ACKNOWLEDGED_NAP,

        UNKNOWN,

        _UNKNOWN
    }

    public interface Visitor<T> {
        T visitLongSleep();

        T visitShortSleep();

        T visitAcknowledgedNap();

        T visitUnknown();

        T visitUnknown(String unknownType);
    }
}
