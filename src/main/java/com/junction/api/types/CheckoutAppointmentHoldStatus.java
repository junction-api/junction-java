/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CheckoutAppointmentHoldStatus {
    public static final CheckoutAppointmentHoldStatus FAILED =
            new CheckoutAppointmentHoldStatus(Value.FAILED, "failed");

    public static final CheckoutAppointmentHoldStatus HELD = new CheckoutAppointmentHoldStatus(Value.HELD, "held");

    public static final CheckoutAppointmentHoldStatus PENDING =
            new CheckoutAppointmentHoldStatus(Value.PENDING, "pending");

    private final Value value;

    private final String string;

    CheckoutAppointmentHoldStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CheckoutAppointmentHoldStatus
                        && this.string.equals(((CheckoutAppointmentHoldStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case FAILED:
                return visitor.visitFailed();
            case HELD:
                return visitor.visitHeld();
            case PENDING:
                return visitor.visitPending();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CheckoutAppointmentHoldStatus valueOf(String value) {
        switch (value) {
            case "failed":
                return FAILED;
            case "held":
                return HELD;
            case "pending":
                return PENDING;
            default:
                return new CheckoutAppointmentHoldStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        PENDING,

        HELD,

        FAILED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitPending();

        T visitHeld();

        T visitFailed();

        T visitUnknown(String unknownType);
    }
}
