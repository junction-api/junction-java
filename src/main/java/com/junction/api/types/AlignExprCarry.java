/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class AlignExprCarry {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private AlignExprCarry(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static AlignExprCarry carryForward(CarryForwardExpr value) {
        return new AlignExprCarry(new CarryForwardValue(value));
    }

    public static AlignExprCarry carryBackward(CarryBackwardExpr value) {
        return new AlignExprCarry(new CarryBackwardValue(value));
    }

    public static AlignExprCarry carryNearest(CarryNearestExpr value) {
        return new AlignExprCarry(new CarryNearestValue(value));
    }

    public boolean isCarryForward() {
        return value instanceof CarryForwardValue;
    }

    public boolean isCarryBackward() {
        return value instanceof CarryBackwardValue;
    }

    public boolean isCarryNearest() {
        return value instanceof CarryNearestValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<CarryForwardExpr> getCarryForward() {
        if (isCarryForward()) {
            return Optional.of(((CarryForwardValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CarryBackwardExpr> getCarryBackward() {
        if (isCarryBackward()) {
            return Optional.of(((CarryBackwardValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CarryNearestExpr> getCarryNearest() {
        if (isCarryNearest()) {
            return Optional.of(((CarryNearestValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AlignExprCarry && value.equals(((AlignExprCarry) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCarryForward(CarryForwardExpr carryForward);

        T visitCarryBackward(CarryBackwardExpr carryBackward);

        T visitCarryNearest(CarryNearestExpr carryNearest);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "mode", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(CarryForwardValue.class),
        @JsonSubTypes.Type(CarryBackwardValue.class),
        @JsonSubTypes.Type(CarryNearestValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("carry_forward")
    @JsonIgnoreProperties("mode")
    private static final class CarryForwardValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "mode", allowSetters = true)
        private CarryForwardExpr value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CarryForwardValue() {}

        private CarryForwardValue(CarryForwardExpr value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCarryForward(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CarryForwardValue && equalTo((CarryForwardValue) other);
        }

        private boolean equalTo(CarryForwardValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AlignExprCarry{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("carry_backward")
    @JsonIgnoreProperties("mode")
    private static final class CarryBackwardValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "mode", allowSetters = true)
        private CarryBackwardExpr value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CarryBackwardValue() {}

        private CarryBackwardValue(CarryBackwardExpr value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCarryBackward(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CarryBackwardValue && equalTo((CarryBackwardValue) other);
        }

        private boolean equalTo(CarryBackwardValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AlignExprCarry{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("carry_nearest")
    @JsonIgnoreProperties("mode")
    private static final class CarryNearestValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "mode", allowSetters = true)
        private CarryNearestExpr value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CarryNearestValue() {}

        private CarryNearestValue(CarryNearestExpr value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCarryNearest(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CarryNearestValue && equalTo((CarryNearestValue) other);
        }

        private boolean equalTo(CarryNearestValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AlignExprCarry{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("mode")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AlignExprCarry{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
