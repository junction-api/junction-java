/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class Select {
    public static final Select SELECT_ALL = new Select(Value.SELECT_ALL, "*");

    private final Value value;

    private final String string;

    Select(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof Select && this.string.equals(((Select) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SELECT_ALL:
                return visitor.visitSelectAll();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static Select valueOf(String value) {
        switch (value) {
            case "*":
                return SELECT_ALL;
            default:
                return new Select(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        SELECT_ALL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitSelectAll();

        T visitUnknown(String unknownType);
    }
}
