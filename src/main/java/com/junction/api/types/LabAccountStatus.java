/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class LabAccountStatus {
    public static final LabAccountStatus PENDING = new LabAccountStatus(Value.PENDING, "pending");

    public static final LabAccountStatus READY_TO_LAUNCH =
            new LabAccountStatus(Value.READY_TO_LAUNCH, "ready_to_launch");

    public static final LabAccountStatus SUSPENDED = new LabAccountStatus(Value.SUSPENDED, "suspended");

    public static final LabAccountStatus ACTIVE = new LabAccountStatus(Value.ACTIVE, "active");

    private final Value value;

    private final String string;

    LabAccountStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof LabAccountStatus && this.string.equals(((LabAccountStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case PENDING:
                return visitor.visitPending();
            case READY_TO_LAUNCH:
                return visitor.visitReadyToLaunch();
            case SUSPENDED:
                return visitor.visitSuspended();
            case ACTIVE:
                return visitor.visitActive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static LabAccountStatus valueOf(String value) {
        switch (value) {
            case "pending":
                return PENDING;
            case "ready_to_launch":
                return READY_TO_LAUNCH;
            case "suspended":
                return SUSPENDED;
            case "active":
                return ACTIVE;
            default:
                return new LabAccountStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        PENDING,

        SUSPENDED,

        READY_TO_LAUNCH,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitPending();

        T visitSuspended();

        T visitReadyToLaunch();

        T visitUnknown(String unknownType);
    }
}
