/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CollectionInferenceSource {
    public static final CollectionInferenceSource DISPATCH_DATE =
            new CollectionInferenceSource(Value.DISPATCH_DATE, "dispatch_date");

    public static final CollectionInferenceSource LAB_ACCESSIONING =
            new CollectionInferenceSource(Value.LAB_ACCESSIONING, "lab_accessioning");

    public static final CollectionInferenceSource SCHEDULED_APPOINTMENT =
            new CollectionInferenceSource(Value.SCHEDULED_APPOINTMENT, "scheduled_appointment");

    public static final CollectionInferenceSource LAB_ARRIVAL =
            new CollectionInferenceSource(Value.LAB_ARRIVAL, "lab_arrival");

    private final Value value;

    private final String string;

    CollectionInferenceSource(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CollectionInferenceSource
                        && this.string.equals(((CollectionInferenceSource) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DISPATCH_DATE:
                return visitor.visitDispatchDate();
            case LAB_ACCESSIONING:
                return visitor.visitLabAccessioning();
            case SCHEDULED_APPOINTMENT:
                return visitor.visitScheduledAppointment();
            case LAB_ARRIVAL:
                return visitor.visitLabArrival();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CollectionInferenceSource valueOf(String value) {
        switch (value) {
            case "dispatch_date":
                return DISPATCH_DATE;
            case "lab_accessioning":
                return LAB_ACCESSIONING;
            case "scheduled_appointment":
                return SCHEDULED_APPOINTMENT;
            case "lab_arrival":
                return LAB_ARRIVAL;
            default:
                return new CollectionInferenceSource(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        LAB_ARRIVAL,

        LAB_ACCESSIONING,

        SCHEDULED_APPOINTMENT,

        DISPATCH_DATE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitLabArrival();

        T visitLabAccessioning();

        T visitScheduledAppointment();

        T visitDispatchDate();

        T visitUnknown(String unknownType);
    }
}
