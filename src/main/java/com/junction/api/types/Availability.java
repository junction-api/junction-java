/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class Availability {
    public static final Availability AVAILABLE = new Availability(Value.AVAILABLE, "available");

    public static final Availability UNAVAILABLE = new Availability(Value.UNAVAILABLE, "unavailable");

    private final Value value;

    private final String string;

    Availability(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof Availability && this.string.equals(((Availability) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case AVAILABLE:
                return visitor.visitAvailable();
            case UNAVAILABLE:
                return visitor.visitUnavailable();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static Availability valueOf(String value) {
        switch (value) {
            case "available":
                return AVAILABLE;
            case "unavailable":
                return UNAVAILABLE;
            default:
                return new Availability(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        AVAILABLE,

        UNAVAILABLE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAvailable();

        T visitUnavailable();

        T visitUnknown(String unknownType);
    }
}
