/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.junction.api.core.Nullable;
import com.junction.api.core.NullableNonemptyFilter;
import com.junction.api.core.ObjectMappers;
import java.time.OffsetDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = OrderTrackingEstimate.Builder.class)
public final class OrderTrackingEstimate {
    private final Optional<OffsetDateTime> originalEstimate;

    private final Optional<OffsetDateTime> currentEstimate;

    private final Map<String, Object> additionalProperties;

    private OrderTrackingEstimate(
            Optional<OffsetDateTime> originalEstimate,
            Optional<OffsetDateTime> currentEstimate,
            Map<String, Object> additionalProperties) {
        this.originalEstimate = originalEstimate;
        this.currentEstimate = currentEstimate;
        this.additionalProperties = additionalProperties;
    }

    @JsonIgnore
    public Optional<OffsetDateTime> getOriginalEstimate() {
        if (originalEstimate == null) {
            return Optional.empty();
        }
        return originalEstimate;
    }

    @JsonIgnore
    public Optional<OffsetDateTime> getCurrentEstimate() {
        if (currentEstimate == null) {
            return Optional.empty();
        }
        return currentEstimate;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("original_estimate")
    private Optional<OffsetDateTime> _getOriginalEstimate() {
        return originalEstimate;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("current_estimate")
    private Optional<OffsetDateTime> _getCurrentEstimate() {
        return currentEstimate;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof OrderTrackingEstimate && equalTo((OrderTrackingEstimate) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(OrderTrackingEstimate other) {
        return originalEstimate.equals(other.originalEstimate) && currentEstimate.equals(other.currentEstimate);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.originalEstimate, this.currentEstimate);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<OffsetDateTime> originalEstimate = Optional.empty();

        private Optional<OffsetDateTime> currentEstimate = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(OrderTrackingEstimate other) {
            originalEstimate(other.getOriginalEstimate());
            currentEstimate(other.getCurrentEstimate());
            return this;
        }

        @JsonSetter(value = "original_estimate", nulls = Nulls.SKIP)
        public Builder originalEstimate(Optional<OffsetDateTime> originalEstimate) {
            this.originalEstimate = originalEstimate;
            return this;
        }

        public Builder originalEstimate(OffsetDateTime originalEstimate) {
            this.originalEstimate = Optional.ofNullable(originalEstimate);
            return this;
        }

        public Builder originalEstimate(Nullable<OffsetDateTime> originalEstimate) {
            if (originalEstimate.isNull()) {
                this.originalEstimate = null;
            } else if (originalEstimate.isEmpty()) {
                this.originalEstimate = Optional.empty();
            } else {
                this.originalEstimate = Optional.of(originalEstimate.get());
            }
            return this;
        }

        @JsonSetter(value = "current_estimate", nulls = Nulls.SKIP)
        public Builder currentEstimate(Optional<OffsetDateTime> currentEstimate) {
            this.currentEstimate = currentEstimate;
            return this;
        }

        public Builder currentEstimate(OffsetDateTime currentEstimate) {
            this.currentEstimate = Optional.ofNullable(currentEstimate);
            return this;
        }

        public Builder currentEstimate(Nullable<OffsetDateTime> currentEstimate) {
            if (currentEstimate.isNull()) {
                this.currentEstimate = null;
            } else if (currentEstimate.isEmpty()) {
                this.currentEstimate = Optional.empty();
            } else {
                this.currentEstimate = Optional.of(currentEstimate.get());
            }
            return this;
        }

        public OrderTrackingEstimate build() {
            return new OrderTrackingEstimate(originalEstimate, currentEstimate, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
