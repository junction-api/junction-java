/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.junction.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class OrderActivationType {
    public static final OrderActivationType CURRENT = new OrderActivationType(Value.CURRENT, "current");

    public static final OrderActivationType SCHEDULED = new OrderActivationType(Value.SCHEDULED, "scheduled");

    private final Value value;

    private final String string;

    OrderActivationType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof OrderActivationType && this.string.equals(((OrderActivationType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CURRENT:
                return visitor.visitCurrent();
            case SCHEDULED:
                return visitor.visitScheduled();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static OrderActivationType valueOf(String value) {
        switch (value) {
            case "current":
                return CURRENT;
            case "scheduled":
                return SCHEDULED;
            default:
                return new OrderActivationType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        CURRENT,

        SCHEDULED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCurrent();

        T visitScheduled();

        T visitUnknown(String unknownType);
    }
}
